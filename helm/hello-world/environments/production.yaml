environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2101m
    memory: 1083Mi
  requests:
    cpu: 1050m
    memory: 833Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
