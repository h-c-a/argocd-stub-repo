environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2858m
    memory: 3884Mi
  requests:
    cpu: 1429m
    memory: 2988Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
