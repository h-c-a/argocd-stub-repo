environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2271m
    memory: 1207Mi
  requests:
    cpu: 1135m
    memory: 928Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
