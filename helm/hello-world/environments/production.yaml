environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2923m
    memory: 4371Mi
  requests:
    cpu: 1461m
    memory: 3362Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
