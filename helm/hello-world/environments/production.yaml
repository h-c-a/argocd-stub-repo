environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2155m
    memory: 1300Mi
  requests:
    cpu: 862m
    memory: 1000Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
