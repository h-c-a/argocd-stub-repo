environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2931m
    memory: 4275Mi
  requests:
    cpu: 1465m
    memory: 3288Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
