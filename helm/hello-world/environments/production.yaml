environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 3029m
    memory: 5525Mi
  requests:
    cpu: 1514m
    memory: 4250Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
