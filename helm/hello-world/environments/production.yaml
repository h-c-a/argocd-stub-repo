environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 4714m
    memory: 3932Mi
  requests:
    cpu: 2357m
    memory: 3024Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
