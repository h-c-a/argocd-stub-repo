environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2890m
    memory: 4160Mi
  requests:
    cpu: 1445m
    memory: 3200Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
