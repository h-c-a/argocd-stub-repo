environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 3008m
    memory: 5393Mi
  requests:
    cpu: 1504m
    memory: 4148Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
