environment: production

replicaCount: 3

image:
  repository: polinux/stress-ng
  tag: "latest"
  pullPolicy: IfNotPresent

resources:
  limits:
    cpu: 2868m
    memory: 3913Mi
  requests:
    cpu: 1434m
    memory: 3010Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 6 
